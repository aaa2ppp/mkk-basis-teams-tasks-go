## TODO: Рефакторинг аутентификации и управления ролями

- [ ] **Перейти с шифрования (JWE) на подпись (JWS)** с использованием симметричного ключа (HMAC) для упрощения и ускорения валидации.
- [ ] **Внедрить Redis-кеш для хранения полной информации о пользователе (включая роли)** с отдельными ключами на каждого пользователя и настраиваемым TTL. Реализовать загрузку из БД при промахе.
- [ ] **Добавить механизм инвалидации кеша** при изменениях членства в командах (создание команды, добавление участника, смена роли) – предоставить teams-пакету интерфейс/функцию `InvalidateUser(ctx, userID)`.
- [ ] **Переработать HTTP-мидлвару** – проверять JWT, извлекать UserID, загружать пользователя из кеша (при необходимости — из БД) и помещать в контекст.
- [ ] **Адаптировать методы `Login` и `Register`** – убрать загрузку ролей на этапе аутентификации, генерировать токен только по UserID.
- [ ] **Обеспечить согласованность данных**: при любых изменениях, влияющих на пользователя (смена пароля, удаление), также инвалидировать кеш.
- [ ] **Продумать миграцию** при переходе на новый формат токенов (временная поддержка обоих форматов или принудительный перевыпуск).
- [ ] **Определить зависимости между пакетами** так, чтобы teams мог вызывать инвалидацию, но auth не зависел от teams – это позволит избежать циклических связей и сохранить гибкость.